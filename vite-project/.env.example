# Anthropic API Key
# Note: Currently the app calls the Anthropic API directly from the client.
# In production, this should be moved to a backend proxy for security.
# VITE_ANTHROPIC_API_KEY=your_api_key_here
