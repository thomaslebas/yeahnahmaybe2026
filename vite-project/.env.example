# Chat API proxy URL (Cloudflare Worker)
# Local dev: use your deployed worker URL, or run the worker locally with wrangler dev
VITE_CHAT_API_URL=https://yeahnahmaybe-chat.<your-subdomain>.workers.dev
